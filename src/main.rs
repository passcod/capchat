#[tokio::main]
async fn main() {
	println!("Hello, world!");

	// parse arguments

	// fetch rss feed
	// parse
	// check cache database for already-seen items
	// fetch the rest
	// store in cache database

	// parse local geojson of areas we care about
	// parse items polys
	// filter for intersections and levels we care about
	// prepare for display
	// print out
	// make call to chat api (in prod)
}
